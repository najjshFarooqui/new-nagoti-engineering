<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="item"
            type="com.example.applligent.newnagoriengg.Item" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingLeft="32dp"
        android:paddingRight="32dp"
        tools:context=".UpdateActivity">

        <TextView
            android:id="@+id/infoText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="update information"
            android:textSize="24sp"
            android:layout_marginTop="24dp"/>
<!--
        <EditText
            android:id="@+id/fieldCompanyName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@={item.companyName}"
            android:layout_marginTop="24dp"/>

        <EditText
            android:id="@+id/fieldPartName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@={item.partName}"
            android:layout_marginTop="24dp"/>

        <EditText
            android:id="@+id/fieldPartNumber"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@={item.partNumber}"
            android:layout_marginTop="24dp"/>

        <EditText
            android:id="@+id/fieldPriceRate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="price"
            android:text="@={`` + item.priceRate}"
            android:layout_marginTop="24dp"/>-->
        <Button
            android:id="@+id/updateButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Update"
            android:layout_marginTop="32dp"
            android:layout_gravity="center_horizontal"/>
    </LinearLayout>
</layout>


    <!-- public int id;public String companyName;public String partName;public  String partNumber;public float priceRate;-->